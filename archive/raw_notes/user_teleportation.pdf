import random

def quantum_teleportation_demo():
    print("=" * 80)
    print("🛰️ QUANTUM TELEPORTATION: Beaming a 'Thought'")
    print("=" * 80)
    
    # 1. THE THOUGHT (Alice's secret state)
    # This represents a complex quantum decision before it's measured
    thought_state = "COMPLEX_SUPERPOSITION_X"
    print(f"1. Alice has a secret 'Thought' (State: {thought_state})")
    
    # 2. ENTANGLEMENT (The Bridge)
    # Alice and Bob share an entangled pair (Example 2)
    # q_alice and q_bob are now connected across space
    print("2. Alice and Bob share an entangled 'Bridge' across space...")
    
    # 3. THE BELL MEASUREMENT
    # Alice interacts her 'Thought' with her half of the bridge.
    # This 'collapses' the thought on her end.
    print("3. Alice measures her Thought against the Bridge.")
    print("   💥 Alice's original thought is DESTROYED (No Cloning Theorem).")
    
    # Alice gets two classical bits from her measurement
    classical_bits = (random.randint(0, 1), random.randint(0, 1))
    print(f"   Alice sends Bob two classical bits: {classical_bits}")
    
    # 4. THE RECONSTRUCTION
    # Bob uses Alice's bits to 'rotate' his half of the bridge
    print("4. Bob applies rotations based on Alice's bits.")
    
    # 5. THE RESULT
    # Bob's qubit now holds the EXACT state Alice started with
    bobs_state = thought_state
    print(f"\n✨ SUCCESS: Bob now has the 'Thought' (State: {bobs_state})")
    print("   The thought moved from Alice to Bob WITHOUT passing through space!")

print("RUNNING YOUR QUANTUM TELEPORTATION CODE")
print("=" * 80)
print()

# Run it 3 times to show different classical bit outcomes
for i in range(3):
    print(f"\n--- Trial {i+1} ---")
    quantum_teleportation_demo()
    print()
